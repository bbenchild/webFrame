<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN" "http://struts.apache.org/dtds/struts-2.3.dtd">
<!-- struts的配置 -->
<struts>
	<!-- 请求参数的编码方式 -->
	<constant name="struts.i18n.encoding" value="UTF-8" />
	<!-- 指定被struts2处理的请求后缀类型(即xxx.action)。多个用逗号隔开 -->
	<constant name="struts.action.extension" value="action,do,htm" />
	<!-- 当struts.xml改动后，是否重新加载。默认值为false(生产环境下使用),开发阶段最好打开 -->
	<constant name="struts.configuration.xml.reload" value="true" />
	<!-- 是否使用struts的开发模式。开发模式会有更多的调试信息。默认值为false(生产环境下使用),开发阶段最好打开 -->
	<constant name="struts.devMode" value="true" />
	<!-- 设置浏览器是否缓存静态内容。默认值为true(生产环境下使用),开发阶段最好关闭 -->
	<constant name="struts.serve.static.browserCache" value="false" />
	<!-- 指定由spring负责action对象的创建 -->
	<constant name="struts.objectFactory" value="spring" />
	<!-- 是否开启动态方法调用 -->
	<constant name="struts.enable.DynamicMethodInvocation" value="false" />
	<!-- 临时文件 <constant name="struts.multipart.saveDir" value="/tmp"/> -->
	<!-- 上传文件大小 -->
	<constant name="struts.multipart.maxSize" value="104857600" />

	<!-- 需要时配置相应的action -->
	<package name="defaultPackage" namespace="/" extends="struts-default">
		<!-- 测试用的action,当与Spring整合后，class属性就是bean的名称 -->
		<action name="login" class="com.z2.Action.LoginAction" method="login">
			<result name="loginSuccess">/loginsuccess.jsp</result>
			<result name="loginFailure">/loginfailure.jsp</result>
		</action>
		
				<action name="register" class="com.z2.Action.LoginAction" method="register">
			<result name="registerSuccess">/registersuccess.jsp</result>
			<result name="registerFailure">/registerfailure.jsp</result>
		</action>
		
	</package>
</struts>